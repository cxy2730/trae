[package]
name = "netbios_stub"
version = "1.0.0"
edition = "2021"

[lib]
name = "netbios"
crate-type = ["cdylib"]

[dependencies]

[profile.release]
opt-level = "z"
lto = true
panic = "abort"
strip = true
codegen-units = 1
